## init_sql() テスト・修正記録


### 1回目：フォルダ名を誤って指定

実行：
`init_sql EXITS init_reset`

結果：
- 引数 `$1`、`$2` の入力を正常に受付
- `folder` に `EXITS` を格納
- `table_name` に `init_reset` を格納
- 入力内容の確認メッセージが正常に表示された
- 指定したフォルダに `CREATE.sql` が存在しないことを検出
- 「CREATE.sql が存在しません」と表示された
- 後続処理へ進まず終了

判定：`正常`

### 2回目：正しいフォルダ名・テーブル名を指定

実行：
`init_sql EXISTS init_reset`

結果：
- 引数 `$1`、`$2` を正常に受付
- 入力内容の確認メッセージが正常に表示された
- `init_reset` テーブルが存在しないことを確認
- テーブル作成処理へ進んだ
- `CREATE.sql` を実行
- `Meetings`、`TestScore` テーブルが既に存在するため `CREATE TABLE` に失敗
- CREATE.sqlの実行失敗を検出
- 失敗メッセージを表示して処理を終了

判定：`正常`

確認事項：
- テーブル存在確認は指定した1テーブルのみを対象としている
- CREATE.sql実行時はファイル内のCREATE文をすべて実行している
- 存在確認の単位とCREATE.sql実行の単位が一致していない
- 複数テーブルを作成するCREATE.sqlに対応するため、存在確認方法または実行方法の見直しが必要

### init_sql SQL実行方法の修正

#### 修正前

現在は、以下の処理でSQLファイル全体を実行している。

```bash
sqlite3 "${DB}" < "${create_file}"
sqlite3 "${DB}" < "${insert_file}"
```

この処理では、`CREATE.sql` および `INSERT.sql` に記述されているSQLが、先頭から最後まで実行される。

そのため、指定したテーブル以外の `CREATE TABLE` 文や `INSERT` 文も実行対象となる。

既に存在するテーブルに対して再度 `CREATE TABLE` を実行した場合、テーブル重複エラーが発生する。

---

#### 修正方針

`CREATE.sql` および `INSERT.sql` 全体を実行するのではなく、指定したテーブルに関係するSQLのみを取得して実行する。

---

#### CREATE.sql の修正内容

- `CREATE.sql` を読み込む
- `CREATE TABLE ${table_name}` を検索する
- 対象の `CREATE TABLE` 文の開始位置から、文末の `;` までを取得する
- 取得したSQLのみを `sqlite3` に渡して実行する

---

#### INSERT.sql の修正内容

- `INSERT.sql` を読み込む
- `${table_name}` を対象とする `INSERT` 文を検索する
- 対象テーブルの `INSERT` 文のみを取得する
- 取得したSQLのみを `sqlite3` に渡して実行する

---

#### 修正後の動作

- `CREATE.sql` から指定テーブルの `CREATE TABLE` 文のみ実行する
- `INSERT.sql` から指定テーブルの `INSERT` 文のみ実行する
- 指定していないテーブルのSQLは実行しない
- 既存テーブルに対する不要な `CREATE TABLE` の再実行を防止する
- 指定していないテーブルへの不要なデータ登録を防止する


#### 対象SQL抽出処理追加後のエラー判定について

`CREATE.sql` および `INSERT.sql` 全体を実行する処理から、`${table_name}` を基準に対象テーブルのSQLのみを抽出して実行する処理へ修正した。

しかし、対象SQLを抽出する処理を追加したことで、指定したテーブル名のSQLがファイル内に存在しない場合の判定が必要になった。

現在は、`awk` によるSQL抽出後に `sqlite3` の終了ステータスのみで成功・失敗を判定している。

対象となる `CREATE TABLE` 文や `INSERT` 文が存在しない場合、`awk` は何も出力しない。

その場合でも `sqlite3` は空の入力を正常終了として処理する可能性があり、対象SQLを実行していないにもかかわらず、SQLの実行に成功したと誤判定する可能性がある。


#### 修正方針

SQLを抽出・実行する前に、対象テーブルのSQLがファイル内に存在するか確認する。


##### CREATE.sql

- `CREATE.sql` 内に `CREATE TABLE ${table_name}` が存在するか確認する
- 存在しない場合はエラーメッセージを表示する
- `exit 1` でエラー終了する
- 存在する場合のみ、対象の `CREATE TABLE` 文を抽出して実行する


##### INSERT.sql

- `INSERT.sql` 内に `${table_name}` を対象とする `INSERT` 文が存在するか確認する
- 存在しない場合はエラーメッセージを表示する
- `exit 1` でエラー終了する
- 存在する場合のみ、対象の `INSERT` 文を抽出して実行する


#### 修正後の判定

```text
対象SQLが存在するか確認
↓
存在しない
↓
エラーメッセージを表示
↓
exit 1

対象SQLが存在する
↓
awkで対象SQLを抽出
↓
sqlite3で実行
↓
終了ステータスでSQL実行結果を判定
```

これにより、SQLの存在確認とSQLの実行結果を別々に判定し、対象SQLが存在しない場合の成功誤判定を防止する。

#### SQL抽出結果の判定・出力先の修正

`awk` の抽出結果を直接 `sqlite3` に渡していたため、SQL抽出とSQL実行の成功・失敗を別々に判定できなかった。

#### 修正内容

- `awk` の抽出結果を変数に格納
- 終了ステータスからSQL抽出結果を判定
- エラーメッセージを `/dev/stderr` へ出力
- 抽出成功時のみ `sqlite3` でSQLを実行

これにより、SQL抽出とSQL実行を別々に判定し、SQLとエラーメッセージの混在を防止する。

### 3回目：引数指定・テーブル作成

#### 実行内容

```bash
init_sql EXISTS init_reset_test
```

#### 実行結果

- フォルダ名・テーブル名を引数から取得
- 対象テーブルが存在しないことを確認
- 対象のCREATE文を正常に抽出
- CREATE文の実行に成功
- 対象のINSERT文を正常に抽出
- INSERT文の実行に成功
- テーブル作成処理が正常終了

#### テスト結果

正常終了を確認。

※ 現在はINSERT文1件で確認。複数INSERT文の抽出・実行は別途テストする。

### 4回目：複数INSERT文の実行

#### 実行内容

同一テーブルに対して複数の `INSERT` 文を記述し、`init_sql` を実行。

```sql
INSERT INTO init_reset_test3 VALUES ('3');
INSERT INTO init_reset_test3 VALUES ('a3');
```

#### 実行結果

- 対象テーブルのCREATE文を正常に抽出・実行
- 同一テーブルに対する複数のINSERT文を正常に抽出
- 複数INSERT文の実行に成功
- `SELECT` により2件のデータ登録を確認

```sql
3
a3

```


### 5回目：引数なし・readによる入力

#### 実行内容

引数を指定せず `init_sql` を実行し、入力待ち後にフォルダ名・テーブル名を入力。

#### 実行結果

- `read` による入力受付を確認
- 入力値がフォルダ名・テーブル名として正常に取得された
- CREATE文の抽出・実行に成功
- 複数INSERT文の抽出・実行に成功
- `SELECT` により2件のデータ登録を確認

#### テスト結果

引数なしの場合でも、入力受付後にテーブル作成・データ登録が正常終了することを確認。

### 6回目：対象CREATE文が存在しない場合

#### 実行内容

`CREATE.sql` 内に存在しないテーブル名を指定して `init_sql` を実行。

#### 実行結果

- 指定テーブルがDBに存在しないことを確認
- `CREATE.sql` から対象CREATE文を検索
- 対象CREATE文が存在しないことを検出
- エラーメッセージを表示
- CREATE文の抽出失敗として処理を終了

#### テスト結果

対象CREATE文が存在しない場合、エラーを正常に検出して処理を終了することを確認。


### 7回目：対象テーブルが既に存在する場合

#### 実行内容

DB内に既に存在するテーブル名を指定して `init_sql` を実行。

#### 実行結果

- 対象テーブルの存在を確認
- CREATE・INSERT処理を実行せず終了
- 既存テーブルへの不要な再作成・データ登録を防止

#### テスト結果

対象テーブルが既に存在する場合、そのまま正常終了することを確認。

### 8回目：対象INSERT文が存在しない場合

#### 実行内容

対象テーブルのCREATE文のみ存在し、対応するINSERT文が存在しない状態で `init_sql` を実行。

#### 実行結果

- CREATE文の抽出・実行に成功
- `INSERT.sql` から対象INSERT文を検索
- 対象INSERT文が存在しないことを検出
- エラーメッセージを表示
- INSERT文の抽出失敗として処理を終了

#### テスト結果

対象INSERT文が存在しない場合、エラーを正常に検出して処理を終了することを確認。

#### INSERT処理失敗時の作成済みテーブル削除

`CREATE` 文の実行成功後に、対象 `INSERT` 文が存在しない場合や `INSERT` 文の実行に失敗した場合、作成済みテーブルがDB内に残ることを確認した。

機能としては作成失敗で終了しているにもかかわらず、途中状態のテーブルが残るため修正する。

#### 改善方針

- INSERT文の抽出に失敗した場合、今回作成した対象テーブルを削除する
- INSERT文の実行に失敗した場合も対象テーブルを削除する
- テーブル削除後にエラー終了する
- `init_sql` が失敗した場合、処理開始前の状態に戻す

これにより、処理途中で作成された不完全なテーブルがDB内に残ることを防止する。

### 9回目：INSERT文抽出失敗時のテーブル削除

#### 実行結果

- CREATE文の抽出・実行に成功
- 対象INSERT文が存在しないことを検出
- INSERT文の抽出失敗として処理を終了
- 作成済みテーブルを削除
- `sqlite_master` を確認し、対象テーブルがDB内に残っていないことを確認

#### テスト結果

INSERT文の抽出に失敗した場合、作成途中のテーブルを削除して処理開始前の状態へ戻すことを確認。

## init_sql テスト・修正結果まとめ

正常系・異常系テストを実施し、対象SQLの抽出、SQL実行、複数INSERT文の実行が正常に動作することを確認した。

また、対象SQLが存在しない場合のエラー判定を追加し、INSERT処理失敗時は作成済みテーブルを削除することで、不完全な状態のテーブルがDB内に残らないよう修正した。

以上により、`init_sql` のテーブル作成処理が想定どおり動作することを確認した。