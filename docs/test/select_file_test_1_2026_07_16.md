## reset_table() テスト・修正記録

### 1回目：引数なし・Enterのみ入力した場合

#### 実行内容

`select_file` を引数なしで実行し、入力待ちで Enter のみを入力。

#### 実行結果

- `folder`、`file_nm` に空文字が格納された
- 対象ファイルパスとして `${BASE_DIR}//` が生成された
- `test -f` による存在確認を実行
- 対象ファイルが存在しないと判定
- エラーメッセージを表示
- `exit 1` で処理を終了


### 2回目：引数指定・実行前確認で「n」を入力した場合

#### 実行内容

`select_file sql/EXISTS SELECT.sql` を実行し、実行方法に `sql`、実行確認で `n` を入力。

#### 実行結果

- 引数から `folder` と `file_nm` を取得
- 対象ファイルの絶対パスを生成
- `test -f` による存在確認を実行
- 対象ファイルが存在すると判定
- 実行方法に `sql` を選択
- 実行前確認で `n` を入力
- SQLファイルは実行せず処理を終了
- `exit 0` で正常終了


### 3回目：引数指定・SQLファイルを正常実行した場合

#### 実行内容

`select_file sql/EXISTS SELECT.sql` を実行し、実行方法に `sql`、実行確認で `y` を入力。

#### 実行結果

- 引数から `folder` と `file_nm` を取得
- 対象ファイルの絶対パスを生成
- `test -f` による存在確認を実行
- 対象ファイルが存在すると判定
- 実行方法に `sql` を選択
- 実行前確認で `y` を入力
- `sqlite3` により対象SQLファイルを実行
- SQLの実行結果を表示
- 実行成功メッセージを表示
- `exit 0` で正常終了


### 4回目：引数なし・Shellスクリプトを正常実行した場合

#### 実行内容

`select_file` を引数なしで実行し、入力待ちで `pubfun gsave.sh` を入力。
実行方法に `bash`、実行確認で `y` を入力。

#### 実行結果

- 入力値から `folder` と `file_nm` を取得
- 対象ファイルの絶対パスを生成
- `test -f` による存在確認を実行
- 対象ファイルが存在すると判定
- 実行方法に `bash` を選択
- 実行前確認で `y` を入力
- `bash` により対象Shellスクリプトを実行
- `gsave.sh` の処理が正常に実行された
- Gitへのコミット・Pushが正常に完了
- 実行成功メッセージを表示
- `exit 0` で正常終了

### 5回目：存在しないフォルダを指定した場合

#### 実行内容

`select_file` を引数なしで実行し、入力待ちで存在しないフォルダ名とファイル名を入力。

```
AAAAAAAAAAAAAAAAAAAA CREATE.sql
```

#### 実行結果

- 入力値から `folder` と `file_nm` を取得
- 対象ファイルの絶対パスを生成
- `test -f` による存在確認を実行
- 対象ファイルが存在しないと判定
- エラーメッセージを表示
- `exit 1` で処理を終了


### 6回目：存在しないファイルを指定した場合

#### 実行内容

`select_file` を引数なしで実行し、入力待ちで存在するフォルダ名と存在しないファイル名を入力。

```
pubfun AAAAAAAAAAAAAAAAAAAAAA.sssh
```

#### 実行結果

- 入力値から `folder` と `file_nm` を取得
- 対象ファイルの絶対パスを生成
- `test -f` による存在確認を実行
- 対象ファイルが存在しないと判定
- エラーメッセージを表示
- `exit 1` で処理を終了

### 7回目：入力待ちで空入力した場合

#### 実行内容

`select_file` を引数なしで実行し、入力待ちで Enter のみを入力。

#### 実行結果

- 引数が指定されていないため入力待ち処理を実行
- `folder` と `file_nm` の入力値を確認
- 入力値が空と判定
- エラーメッセージを表示
- `exit 1` で処理を終了


## select_file テスト・修正結果まとめ

正常系・異常系テストを実施し、対象ファイルの選択、存在確認、SQLファイル・Shellスクリプトの実行が正常に動作することを確認した。

また、入力待ち時の空入力判定を追加し、存在しないフォルダ・ファイルのエラー処理、実行前確認（y/n）、呼び出し先スクリプトの終了ステータスによる成功・失敗判定が正常に動作することを確認した。

以上により、`select_file` が任意ファイル実行の共通基盤として想定どおり動作することを確認した。