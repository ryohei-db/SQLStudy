## DB変数

### 役割

- スクリプトファイルごとに定義するDBパス用の変数
- そのファイルで使用するDBファイルまでの絶対パスを管理する


### この方針にした理由

- スクリプトの格納場所が異なるため、`BASE_DIR` はスクリプトごとに定義する
- 各スクリプトが独立して動作できるようにする
- 他のスクリプトとの依存関係をなくす


### ポイント

- `BASE_DIR` を基準にDBファイルの絶対パスを作成する
- カレントディレクトリ（現在位置）の影響を受けない
- パスのずれを防ぎ、安全にDBを扱える
