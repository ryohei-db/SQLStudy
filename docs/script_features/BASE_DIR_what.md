## BASE_DIR変数

### 役割

- スクリプトファイルが格納されているディレクトリの絶対パスを取得する
- スクリプトごとの基準となるディレクトリを決定する

### この変数を使う理由

- スクリプトファイルの格納場所を基準にパスを設計するため
- 呼び出されたスクリプトの格納場所を常に参照する
- カレントディレクトリ（現在位置）の影響を受けない
- 相対パスのずれを防ぐ
- パスを安全かつ統一して管理できる

### ポイント

### ポイント

- 実行場所ではなく、スクリプトの格納場所を基準にする
- `BASE_DIR` を基準にDBや各種ファイルパスを設計する