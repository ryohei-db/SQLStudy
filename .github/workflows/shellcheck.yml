# このGitHub Action(ワークフロー) につける名前
# このワークフローの名前は shellcheck

name: shellcheck

on:

  push:

jobs:

  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: find . -name "*.sh" | xargs shellcheck





  

  









