# SQLStudy 開発ロードマップ

SQLStudyを、自身のSQL・Linux学習環境から、第三者も利用できるSQL支援ツールへ段階的に発展させる。

---

## Phase 1：環境依存の解消・汎用化

現在の自分専用環境に依存した構成を見直し、第三者のLinux環境でも利用できる状態にする。

- 固定パスの解消
- `.bashrc` 依存の解消
- 設定ファイルの汎用化
- 実行入口の見直し
- 初期セットアップ処理の整備
- 第三者環境での動作確認

---

## Phase 2：SQLiteリアルタイムチェック

SQLiteのSQLを対象としたチェック機能を追加する。

SQL作成時に構文上の問題を確認しやすい環境を構築する。

---

## Phase 3：JavaによるGUI化

CLIで提供している各機能をJavaから操作できるGUIを開発する。

CLI操作に慣れていない利用者でも扱いやすいツールへ発展させ、公開リリースを目指す。

---

## Phase 4：Python × AIによるSQL生成

PythonとAIを利用したSQL生成・入力支援機能を追加する。

利用者の入力内容からSQLを生成し、既存のSQL実行・チェック機能と連携できる構成を目指す。

---

## 最終目標

SQLの作成・チェック・実行を支援できるツールとして長期的に開発を継続し、オープンソースとして第三者が利用・改善に参加できるプロジェクトを目指す。